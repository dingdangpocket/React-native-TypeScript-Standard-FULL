export interface Overlay {
  name: string;
  imagePath: any;
  coords: [number, number, number, number];
  color: string;
}

export interface FacadeSiteInfo {
  name: string;
  defectiveLevel: string;
}
